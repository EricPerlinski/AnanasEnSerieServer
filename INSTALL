Suivre les instructions pour installer l'application web :

Lancer la commande composer install, après avoir installé composer (gestionnaire de dépendances PHP).

Créer une base de données pour utiliser l'application.

Configurer la base de données en créant une copie du fichier /app/config/database.php.template
nommée /app/config/database.php .
Adaptant les champs de ce fichier à votre propre configuration.

Vous pouvez maintenant initialiser votre base grâce au fichier base.dump

Vous pouvez finalement déposer tout le contenu du projet dans le dossier visible de votre serveur (/var/www pour apache) via FTP.
