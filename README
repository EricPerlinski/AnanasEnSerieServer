Ce dépôt concerne le serveur PHP.
Le dépôt correspondant au client est disponnible à l'adresse suivante :
https://bitbucket.org/telecomnancy/codingweek_prj11-client

Url disponnibles RELEASE_DAY_3 :

** WEB

- / : qui affiche la page d'accueil et permet de télecharger le client java (pas complète pour le moment).

- /like/path :  permet d'incrementer le nombre de vues d'un QRCode de type "like" et d'afficher un message de remerciement.

#### NEW ####
- /redirect/path :  permet d'incrementer le nombre de vues d'un QRCode de type "redirect" et de rediriger vers le site enregistré dans le QRCode.


- /admin/get/flash/pathAdmin : permet de voir le nombre de votes d'un QRCode de type "like".

#### NEW ####
- /admin/get/redirect/pathAdmin : permet de voir le nombre de votes d'un QRCode de type "redirect".

** API

- /api/admin/add/like : qui prend en paramètres POST un "object", qui sera un objet "Like". Retourne le path et pathAdmin du sondage.

#### NEW ####
- /api/admin/add/redirect : qui prend en paramètres POST un "object", qui sera un objet "Redirect". Retourne le path et pathAdmin du sondage.

- /api/test : retourne le texte "ananas" afin de tester dans le client si l'url du serveur est correct.

Fichiers liés :

- INSTALL permet de spécifier comment installer le projet.
- journal.org indique les tâches restantes, et donne des indicateurs projet.
- contributors.txt contient la liste des membres ayqnt participé à ce projet.
- le dossier conception contient des documents présentant la conception de notre application.
