Le projet est disponnible à l'adresse suivante :
http://codingweek.telecomnancy.eu/projet11

Vous pouvez cependant faire votre propre installation de cette manière :

Lancez la commande composer install, après avoir installé composer (gestionnaire de dépendances PHP).

Créez une base de données pour utiliser l'application.

Configurez la base de données en créant une copie du fichier /app/config/database.php.template
nommée /app/config/database.php .
Adaptez les champs de ce fichier à votre propre configuration.

Vous pouvez maintenant initialiser votre base grâce au fichier base.sql

Vous pouvez finalement déposer tout le contenu du projet dans le dossier visible de votre serveur (/var/www pour apache) via FTP.
